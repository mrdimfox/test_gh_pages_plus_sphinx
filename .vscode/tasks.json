{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "make docs",
            "type": "shell",
            "options": {
                "cwd": "${workspaceFolder}/doc/sphinx"
            },
            "command": "make html",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}